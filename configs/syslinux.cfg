DEFAULT iso-receiver
PROMPT 0
TIMEOUT 30

LABEL iso-receiver
    MENU LABEL ISO Receiver - Sistema de Transferencia
    LINUX /boot/vmlinuz
    INITRD /boot/initramfs.img
    APPEND quiet loglevel=0 vt.global_cursor_default=0
